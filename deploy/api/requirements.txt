# lemur API dependencies, pinned (2026-09).
# The container runs python:3.13-slim; update pins together with the base
# image when either changes.
Flask==3.1.3
Flask-Limiter==4.1.1
pandas==2.3.3
psycopg[binary]==3.2.13
gunicorn==23.0.0